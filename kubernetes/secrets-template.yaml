# This is a template file. Do NOT commit actual secrets to version control.
# Use kubectl create secret or external secret management tools (AWS Secrets Manager, HashiCorp Vault, etc.)
#
# To create the secret manually:
# kubectl create secret generic backend-secrets \
#   --from-literal=db-host=<RDS_ENDPOINT> \
#   --from-literal=db-port=5432 \
#   --from-literal=db-name=employee_management \
#   --from-literal=db-username=<DB_USERNAME> \
#   --from-literal=db-password=<DB_PASSWORD> \
#   --from-literal=jwt-secret=<RANDOM_SECRET> \
#   --from-literal=encryption-key=<RANDOM_KEY>
#
# Or use AWS Secrets Manager with External Secrets Operator:
# https://external-secrets.io/

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app: backend
type: Opaque
stringData:
  # Database configuration
  db-host: "REPLACE_WITH_RDS_ENDPOINT"
  db-port: "5432"
  db-name: "employee_management"
  db-username: "REPLACE_WITH_DB_USERNAME"
  db-password: "REPLACE_WITH_DB_PASSWORD"

  # Application secrets
  jwt-secret: "REPLACE_WITH_RANDOM_JWT_SECRET"
  encryption-key: "REPLACE_WITH_RANDOM_ENCRYPTION_KEY"

  # Third-party API keys (if needed)
  # api-key: "REPLACE_WITH_API_KEY"
---
# Example: Using AWS Secrets Manager with External Secrets Operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: backend-secrets
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: backend-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: db-password
#       remoteRef:
#         key: employee-management/db
#         property: password
#     - secretKey: db-username
#       remoteRef:
#         key: employee-management/db
#         property: username
#     - secretKey: db-host
#       remoteRef:
#         key: employee-management/db
#         property: host
